// Edit these URLs before deploying the extension host.
window.SPECTRAMEDIX_CONFIG = {
  // Public Streamlit app URL (no trailing slash)
  STREAMLIT_URL: "https://YOUR_STREAMLIT_APP.onrender.com",

  // MCP bridge base URL (no trailing slash)
  MCP_BASE_URL: "https://YOUR_MCP_SERVER.onrender.com",

  // How often the extension polls MCP for pending filters (ms)
  POLL_INTERVAL_MS: 2000,
};
